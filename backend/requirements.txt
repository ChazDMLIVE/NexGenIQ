# NexGenIQ backend dependencies.
# Install with:  pip install -r requirements.txt

# --- Web stack --------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pydantic[email]>=2.6
pydantic-settings>=2.2
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt==4.0.1
python-multipart>=0.0.9
httpx>=0.27

# --- Production database driver --------------------------------------
# PostgreSQL driver, used when NEXGENIQ_DATABASE_URL points at Postgres.
# (Local development uses SQLite, which needs no driver.)
psycopg2-binary>=2.9

# --- Testing ----------------------------------------------------------
pytest>=7.0

# Note on the engines: osit-index and osit-sim are NOT listed here.
# They live in the engine/ and sim/ subdirectories of this same
# repository, and are installed by the deploy build step / by a local
# editable install (see ../README.md and ../docs/DEPLOYMENT.md). This
# avoids depending on a relative path or a Git URL at install time.
