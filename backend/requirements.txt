# NexGenIQ backend dependencies (Milestone 1).
# Install with:  pip install -r requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pydantic[email]>=2.6
pydantic-settings>=2.2
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
python-multipart>=0.0.9
httpx>=0.27
pytest>=7.0

# The numerical engine. Installed separately as an editable package:
#   pip install -e ../engine
# (it provides the `osit_index` package).
