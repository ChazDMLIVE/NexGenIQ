# NexGenIQ backend dependencies.
# Install with:  pip install -r requirements.txt
#
# This installs the web stack AND both NexGenIQ engines. The two engine
# packages are installed from their sibling directories in the repo via a
# relative path, so a deployment that clones the repo and runs this file
# gets a complete, working backend with no extra steps.

# --- Web stack --------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pydantic[email]>=2.6
pydantic-settings>=2.2
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
python-multipart>=0.0.9
httpx>=0.27

# --- Production database driver --------------------------------------
# PostgreSQL driver, used when NEXGENIQ_DATABASE_URL points at Postgres.
# (Local development uses SQLite, which needs no driver.)
psycopg2-binary>=2.9

# --- Testing ----------------------------------------------------------
pytest>=7.0

# --- The NexGenIQ engines (installed from the repo) ------------------
# osit-index: the selection-index numerical engine.
# osit-sim:   the herd-simulation engine.
# The -e (editable) install keeps the data/ files discoverable.
-e ../engine
-e ../sim
